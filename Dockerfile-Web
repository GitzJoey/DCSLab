FROM node:20-slim

RUN apt-get update && apt-get install -y \
    git \
    ca-certificates \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

USER node

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host"]
