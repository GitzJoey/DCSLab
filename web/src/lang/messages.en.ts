import buttons from "./en/components/buttons.json"

import login from "./en/views/login.json"

export default {
    "components": {
        "buttons": buttons
    },
    "views": {
        "login": login,
    }
}